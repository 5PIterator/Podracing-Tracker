{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Attach to Outer Wilds",
        "type": "coreclr",
        "request": "attach",
        "processId": "${command:pickProcess}"
      }
    ]
  }
